# 面试项目讲解稿（CADD 虚拟筛选流水线）

配合简历项目使用。目标岗位：CADD/生信/AI for Drug 方向工程师。

---

## 一、30 秒电梯版（开场讲这个）

> 我实现了一个 **EGFR 靶点的虚拟筛选流水线**：用 AutoDock Vina 做分子对接，
> RDKit 处理分子，scikit-learn 做活性预测。输入端是 PubChem 验证过的 6 个已上市
> EGFR 抑制剂 + 共晶配体，跑完后**7 个已知活性分子全部排进前 6-7**，最强 hit 是上市药
> lapatinib（-9.67 kcal/mol），而 10 个非活性对照全部垫底。我用的是真实的 CADD 工具链，
> 整个流程一条命令可复现。

---

## 二、完整 2 分钟版（面试主答）

### 项目动机
虚拟筛选的价值：**不用做实验，用计算机从大量分子里挑出最可能结合的候选**，减少湿实验成本。
但它要可靠，前提是"方法真能区分活性/非活性分子"——我用有已知标签的分子做了验证。

### 我是怎么做的（技术链路）
1. **受体准备**：PDB 4HJO（EGFR 激酶）→ 去掉共晶配体/水 → 干净受体
2. **分子库**：7 个真实 EGFR 抑制剂（6 个已上市药，SMILES 来自 PubChem 官方 API + AQ4 共晶配体）+ 10 个非活性对照
3. **对接**：RDKit 生成 3D 构象 → Meeko 转 PDBQT → AutoDock Vina（官方 Windows exe）批量对接
4. **打分**：取 Vina 多构象最优结合（最低分），盒子中心从 AQ4 共晶配体质心计算
5. **ML 层**：RDKit 描述符 + 随机森林预测活性，与对接互为交叉验证

### 结果（核心）
- 7 个真实 EGFR 药物排进前 6-7（-6.87 ~ -9.67），最强 hit lapatinib -9.67
- 富集倍数 x3（Top 里活性富集），QA 5/5
- 对接打分取最优构象后结果可稳定复现

### 我学到/总结
- Vina 打分是简化模型，用于**相对排序**而非绝对亲和力 → 有用但要诚实
- 负对照要选性质相近的分子，否则"分离效果好"是因为分子差异太大，不代表方法好
- 对接打分波动 → 取多构象最优、固定 exhaustiveness 能改善可复现性

---

## 三、追问防御（面试官最可能问，含标准答法）

### Q1: "你这 7 个活性分子这么容易区分，是不是挑出来的？方法真有效吗？"
- 诚实答："部分对。**7 个活性和 10 个非活性确实能分开**，但我的非活性对照里混了些性质差异大的分子，
  所以分离度有夸张成分。真实虚拟筛选用 DUD-E 这种与活性性质匹配的 decoy 才更严格。
  我这个项目展示的是**流水线能力和方法方向的正确性**，不是声称在生产级筛选中也 1.00。"
- 加分点：主动承认局限 = 你懂行

### Q2: "ML accuracy=1.00 是怎么来的？会不会泄漏？"
- 答："我用**留一交叉验证**，每次留 1 个分子做测试、其余训练，避免了'训练即预测'的泄漏。
  但样本只有 17 个，且负对照性质差异大，所以 1.00 不反映真实世界泛化——我把它当演示，
  不用它宣称方法多准。"
- 关键：**主动说"这个 1.00 不代表生产水平"**，堵住追问。

### Q3: "对接打分为什么波动？可靠吗？"
- 答："Vina 是随机搜索算法，每次会有些波动。我做了两件事缓解：
  取多个构象里的最优结合得分（不是固定第一个 mode），并用较高 exhaustiveness。
  重跑后**排名稳定**，最强 hit 始终是 lapatinib。打分本身是相对排序指标，不是绝对亲和力。"
- 加分点：展示你理解对接的随机性并做可靠性处理

### Q4: "为什么用 AutoDock Vina 不用商业软件（Schrödinger/MOE）？"
- 答："Vina 是开源、可脚本化、面试官容易复现的标准工具，适合做最小可行验证。
  它的打分逻辑（力场+经验项）和商业软件的对接思路一致，迁移到 Glide 等是 workflow 层面的改动。
  用开源工具也保证了这个项目可复现、可二次开发。"

### Q5: "你怎么验证对接盒子是对的？"
- 答："我的盒子中心不是猜的，而是**从 PDB 4HJO 的共晶配体 AQ4 的原子坐标算质心**得来的。
  因为 AQ4 是已知结合 EGFR 的真实配体，它的位置就是结合口袋。这样的盒子能确保候选分子在正确口袋内对接。"

### Q6: "这个和真实的虚拟筛选（筛几十万分子）有什么区别？"
- 答："真实场景会筛分子库（ZINC/Enamine 几百万分子），做分级筛选（初筛 → 精筛 → 打分 → ADMET）。
  我这个是**17 个已知标签分子的验证演示**——证明方法能区分活性/非活性，而不是真的去海量筛新分子。
  代码结构支持扩大库规模（改 config 和 library 即可），但受限于演示数据量。"

---

## 四、面试话术红线（千万别踩）
- ❌ 别说"我的 accuracy=1.00 证明方法很准"（懂行的人会戳）
- ❌ 别说"这是真实 EGFR 抑制剂筛选的成熟方案"（它是验证演示，不是生产筛选）
- ✅ 主动说"这是最小可行验证，展示流水线能力，局限是样本小、需 decoy 严格评估" —— 反而显专业
