services:
  api:
    image: ${BACKEND_IMAGE:?required}
    pull_policy: always

  worker:
    image: ${BACKEND_IMAGE:?required}
    pull_policy: always

  migration:
    image: ${BACKEND_IMAGE:?required}
    pull_policy: always

  recovery-check:
    image: ${BACKEND_IMAGE:?required}
    pull_policy: always

  recovery-evidence-publisher:
    image: ${BACKEND_IMAGE:?required}
    pull_policy: always

  web:
    image: ${FRONTEND_IMAGE:?required}
    pull_policy: always

  plugin-sandbox:
    image: ${BACKEND_IMAGE:?required}
    pull_policy: always
